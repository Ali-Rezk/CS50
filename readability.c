#include <cs50.h>
#include <ctype.h>
#include <string.h>
#include <stdio.h>

int main (void)

{
    string s = get_string ("text: ");

    for (int i=0, len=strlen(s); i<len; i++)
    {
        
    }
}
