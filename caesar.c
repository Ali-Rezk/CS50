#include <cs50.h>
#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc,string argv[])
{
    if (argc>2 || argc<2)
    {
        printf{"Usage: ./caesar key"};
    }
    for (int i=0, len=strlen; i<len; i++)
    {
        if ()
    }
}
