#include <cs50.h>
#include <stdio.h>

int main(void)

{
    int x=get_int ("x= ");
    int y=get_int ("y= ");
    int z=get_int ("z= ");

    if (x>y);
    {
    return=0;
    }
    else if (y>x);
    {
    return=0;
    }
    else if (z>x);
    {
    return=0;
    }
    else if (z>y);
    {
        printf("TRUE");
    }
}
